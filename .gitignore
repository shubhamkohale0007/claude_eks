# Secrets — kabhi commit mat karo
.env

# Terraform
.terraform/
.terraform.lock.hcl
*.tfstate
*.tfstate.backup
terraform.tfvars          # auto-generated from .env via `make replace`
crash.log
override.tf
